# /home/gabriel/.config/tmuxinator/test.yml

name: <%= ENV["session_name"] %>
root: ~/

windows:
  - vpn: sudo openvpn $HOME/sync/Travail/ovpn/dor-gw2-coutug-laptop.ovpn
  - 1k9s: |
      kubie ctx <%= ENV["session_name"] %>
      sleep 2
      clear
      k9s
  - 2k9s: |
      kubie ctx <%= ENV["session_name"] %>
      sleep 2
      clear
      k9s
  - cmd: |
      kubie ctx <%= ENV["session_name"] %>
      clear

